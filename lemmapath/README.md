# LemmaPath

Local-first sentence repetition with TTS + Excel (xlsx) import.

## Run
- npm install
- npm run dev

## Install as an app
- Android / Desktop: use the Install panel if the browser shows it
- iOS Safari: Share → Add to Home Screen

## Notes
- iOS requires one tap to unlock speech before autoplay can work.
- This MVP importer expects headers: English, Target, Transliteration, Gloss, TokenCount, Id
  (You can rename columns or we can add a mapping UI next.)
